import { Section, Template } from "@/components/layout";

/**
 * Homepage
 */
const Homepage = () => {
    return (
        <Template>
            <Section>Hello</Section>
        </Template>
    );
};

export default Homepage;
